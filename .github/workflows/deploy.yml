name: Deploy
on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        required: true
        default: "staging"
        options:
          - staging
          - production

jobs:
  deploy:
    name: "Deploy"
    uses: fxmkdev/webplatform/.github/workflows/export.deploy.yml@v0.9.2
    with:
      environment: ${{ inputs.environment }}
    secrets: inherit
